{{#if label}}
  <label for={{inputId}}>
    {{label}}
  </label>
{{/if}}

{{input
  id=inputId
  type=type
  class=inputClass
  disabled=disabled
  name=name
  placeholder=placeholder
  readonly=readonly
  value=value
}}

{{#if helpText}}
  <p>{{helpText}}</p>
{{/if}}